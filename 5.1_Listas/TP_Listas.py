# Métodos / funciones aplicables a listas: 
    # range(): Se puede crear una lista de números automáticamente utilizando `range(inicio, fin)`.
    # split(): Permite crear una lista a partir de un string, utilizando un caracter específico como separador. Por defecto, utiliza los espacios para separar el string en distintos elementos de la lista.
    # slicing: Es posible acceder a los distintos elementos de una lista utilizando el índice del mismo, de manera análoga a como se realiza con los strings `nombre_lista[índice_elemento]`.
    # append(): Añade un elemento a la lista. Se usa la sentencia `nombreLista.append(elementoAAñadir)`
    # remove(): Elimina un elemento de la lista. Se usa la sentencia `nombreLista.remove(elementoAEliminar)`.

# Además se pueden actualizar elementos que ya contenga la lista con la sentencia:
    #nombre_lista[índice_del_elemento_a_actualizar] = "Actualización"

# También se pueden concatenar dos listas utilizando operador de suma       
    #notas_2 = notas + ["UTN", True]

# Podemos comprobar si un valor existe dentro de la lista utilizando el término "in":
    # Ej: print(8 in notas_2)

# Las listas se pueden anidar, es decir tener una lista dentro de otra:
    # Ej:
    #   lista_anidada = [1, 1.3, ["UTN", "Mendoza"]]
    #   print(lista_anidada)
    #   [1, 1.3, ['UTN', 'Mendoza']]


